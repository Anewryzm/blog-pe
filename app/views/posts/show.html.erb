<p> <%= @post.created_at.strftime('%A, %B, %d')%></p>
<h1>
	<%= @post.title%> <br>
</h1>
<p>
	<%= @post.content %>
</p>

<br>
<br>

<%= link_to "Edit", edit_post_path(@post)%>
<%= link_to "Delete", post_path(@post), method: :delete, data: {confirm: "Do you want to delete this posts?"}%>